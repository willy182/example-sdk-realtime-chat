Example using the chat SDK.

To run locally with the modules in this repo, ensure you're at the repo root and have go 1.25.4:
- `go work init ./chat ./examples/simple` (or use provided go.work)
- `cd examples/simple`
- `go run .`
